@model List<PhoneBookApp.WebUI.Models.ReportDataModel>
@{
    ViewData["Title"] = "Rapor Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-sm-12 col-xl-6">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Student Score Detail By Pie Chart</h6>
                <canvas id="piechart"></canvas>
            </div>
        </div>
    </div>
</div>
<div class="container-fluid pt-4 px-4">
</div>

<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/templates/dashmin/lib/chart/chart.min.js"></script>
<script>
    var piechartCanvas = document.getElementById('piechart');
    var studentname = [];
    var scores = [];
    @if (Model.Count > 0)
    {
        foreach (var item in Model)
        {
            @:studentname.push('@item.Name')
            @:scores.push('@item.Name')
        }
    };
    var piechart = new Chart(piechartCanvas, {
        type: 'pie',
        data: {
            labels: studentname,
            datasets: [{
                data: scores,
                borderWidth: 0,
                backgroundColor: [
                    'rgb(255,99,132)',
                    'rgb(54,162,235)',
                    'rgb(157,230,41)',
                    'rgb(41,224,230)',
                    'rgb(255,205,86)'
                ],
                hoverBackgroundColor: [
                    'rgb(255,99,132)',
                    'rgb(54,162,235)',
                    'rgb(157,230,41)',
                    'rgb(41,224,230)',
                    'rgb(255,205,86)'
                ]
            }]
        },
        options: {
            responsive: true,
            legend: {
                position: "bottom",
                labels: {
                    boxWidth: 11,
                    fontColor: "#757681",
                    fontSize: 11
                }
            }
        }
    });
</script>